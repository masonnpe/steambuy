server:
  port: 8010
spring:
  application:
    name: product-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka